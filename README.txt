Getting Started:
1) run "createKeystore.sh"
2) copy ".keystore" into ~
3) run "build.sh"
4) run "run.sh"
5) if necessary, the password can be reset using
5.1) update users set PASSWORD = sha("new_password") where username = 'username';

